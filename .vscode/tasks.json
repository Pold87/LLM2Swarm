{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run starter script",
            "type": "shell",
            "command": "${workspaceFolder}/LLM-Swarm/starter -s",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
